import os

curr_dir = os.getcwd()

COMMAND = curr_dir + '/Quixote-nethack/bin/nethack'
OPTIONS_FILE = curr_dir + '/Quixote-nethack/quixote.nethackrc'

WIDTH, HEIGHT = 80, 24
READ_TIMEOUT = 0.001
MOVE_DELAY = 0
Batch_size = 4
